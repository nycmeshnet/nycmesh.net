[build]
publish = "public"
command = "sh update-map.sh && sh update-stats.sh && hugo --minify"

[context.production.environment]
HUGO_VERSION = "0.48"
HUGO_ENV = "production"

[context.deploy-preview.environment]
HUGO_VERSION = "0.48"

[context.branch-deploy.environment]
HUGO_VERSION = "0.48"
